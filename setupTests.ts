// Jest Setup for sellor.ai
import '@testing-library/jest-dom/extend-expect';

// Optional: Add any additional setup here, such as polyfills or global mocks.

// Example of adding a global mock:
// jest.mock('some-module', () => ({
//   someFunction: jest.fn(),
// }));